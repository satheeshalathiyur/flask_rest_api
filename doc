
#create table
create table user(
   id INT NOT NULL AUTO_INCREMENT,
   title VARCHAR(100) NOT NULL,
   PRIMARY KEY (id)
);

#alert table
ALTER TABLE user ADD  last_name varchar(255);

#api call
create user (method POST)
curl http://localhost:5000/createuser -d "name=test;color=red" -X POST -v